# Detector fine-tuning data = synthetic arena + real photos (real backgrounds teach the
# detector NOT to fire on lab clutter). Single class 'polyhedron'.
path: /home/user/joon/datasets
train:
  - set1/detector/images/train
  - set1_real/detector/images/train
val:
  - set1/detector/images/val
  - set1_real/detector/images/val
names:
  0: polyhedron
